{
  "name": "davellm-desktop",
  "version": "1.0.0",
  "description": "Electron shell for DaveLLM",
  "main": "desktop/main.js",
  "scripts": {
    "install:macos": "zsh scripts/macos/install-launcher.sh",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^44.0.0"
  }
}
